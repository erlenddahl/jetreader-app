<?xml version="1.0" encoding="utf-8" ?>
<pages:PopupPage 
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
    xmlns:animations="clr-namespace:Rg.Plugins.Popup.Animations;assembly=Rg.Plugins.Popup"
    x:Name="GridEditor"
    BackgroundColor="Transparent"
    BackgroundInputTransparent="False"
    CloseWhenBackgroundIsClicked="True"
    BackgroundClicked="CellEditorPopup_OnBackgroundClicked"
    x:Class="EbookReader.Page.Settings.Popups.CellEditorPopup">

    <!--You can use any elements here which are extended from Xamarin.Forms.View-->
    <Frame Margin="20" VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand" BackgroundColor="#EEEEEE" BorderColor="#555555" CornerRadius="10" HasShadow="True" Opacity="0.75">
        <Grid BindingContext="{Reference GridEditor}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"></ColumnDefinition>
                <ColumnDefinition Width="150"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">Selected cell:</Label>

            <Picker Grid.Row="1" Grid.Column="0" ItemsSource="{Binding Commands}" SelectedItem="{Binding SelectedTapCommand}"></Picker>
            <Picker Grid.Row="1" Grid.Column="1" ItemsSource="{Binding Commands}" SelectedItem="{Binding SelectedPressCommand}"></Picker>

            <Button Grid.Row="2" Grid.Column="0" Text="Increase width" Clicked="IncreaseWidth_Clicked"></Button>
            <Button Grid.Row="2" Grid.Column="1" Text="Decrease width" Clicked="DecreaseWidth_Clicked"></Button>

            <Label Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2">Selected row:</Label>

            <Button Grid.Row="4" Grid.Column="0" Text="Add cell before" Clicked="AddCellBefore_Clicked"></Button>
            <Button Grid.Row="4" Grid.Column="1" Text="Add cell after" Clicked="AddCellAfter_Clicked"></Button>

            <Button Grid.Row="5" Grid.Column="0" Text="Delete cell" Clicked="DeleteCell_Clicked"></Button>
            <Button Grid.Row="5" Grid.Column="1" Text="Delete row" Clicked="DeleteRow_Clicked"></Button>

            <Button Grid.Row="6" Grid.Column="0" Text="Add row above" Clicked="AddRowAbove_Clicked"></Button>
            <Button Grid.Row="6" Grid.Column="1" Text="Add row below" Clicked="AddRowBelow_Clicked"></Button>

            <Button Grid.Row="7" Grid.Column="0" Text="Increase height" Clicked="IncreaseHeight_Clicked"></Button>
            <Button Grid.Row="7" Grid.Column="1" Text="Decrease height" Clicked="DecreaseHeight_Clicked"></Button>

        </Grid>
    </Frame>

</pages:PopupPage>